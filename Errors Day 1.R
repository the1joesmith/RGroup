  # R Group errors
  # Anna Moeller
  # 9/11/2017

  # Object doesn't exist error
  a <- c(1, 4, 6, 2, 6)
  a + b
  
  # Class mismatch error
  b <- c("Anna", "Jenny", "Molly", "Kelsey")
  a + b
  
  # Punctuation errors
  head(a))
  head(a, )
  
  c <- NULL
  for(i in 1:length(a){
    c[i] <- a[i] + 3
  }
  